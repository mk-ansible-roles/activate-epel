galaxy_info:
  author: Markus Koch
  description: Installs EPEL repofile and disables repository
  license: Apache 2.0
  min_ansible_version: 2.2
  platforms:
  - name: EL
    versions: [ 6, 7 ]

  galaxy_tags: [ 'system', 'epel' ]

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
